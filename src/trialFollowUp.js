import { supabase } from './supabase'

const MARK='[TRIAL_RESULT]'
const esc=(s='')=>String(s).replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]))
const norm=s=>String(s||'').toLowerCase().replace(/[^a-z0-9\u3400-\u9fff]/g,'')
const css=`
.tf-backdrop{position:fixed;inset:0;background:#0f172ab8;z-index:99990;display:flex;align-items:center;justify-content:center;padding:5vh 4vw}.tf-card{width:min(620px,100%);max-height:90vh;overflow:auto;-webkit-overflow-scrolling:touch;background:#fff;border-radius:28px;padding:20px;box-shadow:0 24px 70px #0005;color:#172033}.tf-head{display:flex;justify-content:space-between;gap:12px;align-items:center;position:sticky;top:-20px;background:#fff;z-index:4;padding:4px 0 10px}.tf-title{font-size:23px;font-weight:900}.tf-close{border:0;border-radius:50%;width:46px;height:46px;font-size:23px;background:#f1f5f9}.tf-actions,.tf-form{display:grid;gap:10px}.tf-btn{border:1px solid #dbe3ef;background:#fff;border-radius:17px;padding:14px;text-align:left;font-size:17px;font-weight:800;min-height:52px}.tf-sub,.tf-res{display:none;gap:9px}.tf-sub.show,.tf-res.show{display:grid;grid-template-columns:1fr 1fr}.tf-sub button,.tf-save{border:0;border-radius:15px;padding:14px;background:#4f46e5;color:#fff;font-weight:900;font-size:16px}.tf-form input,.tf-form select,.tf-form textarea,.tf-batch-row select{font-size:16px;padding:13px;border:1px solid #cbd5e1;border-radius:14px;background:#fff;width:100%;box-sizing:border-box}.tf-form label{display:block;font-size:13px;font-weight:800;color:#64748b;margin-bottom:5px}.tf-row{display:grid;grid-template-columns:1fr 1fr;gap:10px}.tf-tools{position:fixed;left:16px;bottom:98px;z-index:7800;display:flex;gap:8px}.tf-tool{border:0;border-radius:18px;padding:11px 14px;background:#fff;color:#4f46e5;box-shadow:0 8px 25px #0f172a20;font-weight:900;font-size:14px}.tf-task{background:#4f46e5;color:#fff}.tf-badge{display:inline-flex;min-width:20px;height:20px;padding:0 6px;border-radius:999px;align-items:center;justify-content:center;background:#ef4444;color:#fff;font-size:12px;margin-left:5px}.tf-list{display:grid;gap:10px}.tf-item{border:1px solid #e2e8f0;border-radius:17px;padding:13px;display:flex;gap:10px;justify-content:space-between;align-items:center}.tf-item-name{font-weight:900}.tf-item-meta{font-size:13px;color:#64748b;margin-top:4px}.tf-item button{border:0;border-radius:13px;padding:10px 12px;background:#eef2ff;color:#4338ca;font-weight:800}.tf-section{font-size:14px;font-weight:900;color:#475569;margin:15px 2px 8px}.tf-empty{text-align:center;padding:28px;color:#64748b}.tf-smart{background:#f5f3ff;border:1px dashed #8b5cf6;border-radius:17px;padding:13px}.tf-smart textarea{min-height:90px;resize:vertical}.tf-tabs{display:grid;grid-template-columns:1fr 1fr;gap:8px;margin:4px 0 14px}.tf-tab{border:0;border-radius:14px;padding:12px;font-weight:900;background:#f1f5f9;color:#64748b}.tf-tab.on{background:#4f46e5;color:#fff}.tf-batch{display:none}.tf-batch.on{display:block}.tf-single.off{display:none}.tf-batch-list{display:grid;gap:9px;margin-top:12px}.tf-batch-row{border:1px solid #e2e8f0;border-radius:16px;padding:12px;background:#fff}.tf-batch-row.bad{border-color:#fca5a5;background:#fff7f7}.tf-batch-top{display:flex;justify-content:space-between;gap:10px;align-items:start}.tf-batch-name{font-weight:900}.tf-batch-meta{font-size:13px;color:#64748b;margin-top:3px}.tf-batch-tag{font-size:12px;border-radius:999px;padding:5px 8px;background:#dcfce7;color:#15803d;white-space:nowrap}.tf-batch-row.bad .tf-batch-tag{background:#fee2e2;color:#dc2626}.tf-batch-row select{margin-top:8px;padding:9px;font-size:14px}.tf-batch-summary{font-size:14px;font-weight:800;margin-top:10px}.tf-batch-save{width:100%;margin-top:10px;background:#16a34a}.tf-filter-note{background:#ecfdf5;color:#047857;border-radius:13px;padding:10px 12px;font-size:13px;font-weight:700}.tf-match{font-size:13px;padding:10px 12px;border-radius:12px;background:#eef2ff;color:#4338ca;display:none}.tf-match.show{display:block}
@media(max-width:480px){.tf-card{padding:17px;border-radius:24px}.tf-row{grid-template-columns:1fr}.tf-tools{left:10px;bottom:91px}.tf-tool{padding:10px 12px}.tf-sub.show,.tf-res.show{grid-template-columns:1fr}}
`
const trialTime=c=>{if(!c?.trial_date)return null;const d=new Date(String(c.trial_date).replace(' ','T'));return isNaN(d)?null:d}
const hasResult=c=>String(c?.remark||'').includes(MARK)
const cleanRemark=(r='')=>String(r).replace(/\n?\[TRIAL_RESULT\]\|[^\n]*/g,'').trim()
const marker=(outcome,when=new Date().toISOString(),extra='')=>`${MARK}|${when}|${outcome}${extra?'|'+extra:''}`
const ended=c=>{const d=trialTime(c);return d&&!hasResult(c)&&Date.now()>=d.getTime()&&String(c.status||'prospect').toLowerCase()==='prospect'}
const todayHK=()=>new Intl.DateTimeFormat('en-CA',{timeZone:'Asia/Hong_Kong',year:'numeric',month:'2-digit',day:'2-digit'}).format(new Date())
const isToday=c=>String(c?.trial_date||'').slice(0,10)===todayHK()
const fmt=c=>{const d=trialTime(c);return d?d.toLocaleString('zh-HK',{month:'numeric',day:'numeric',hour:'2-digit',minute:'2-digit',hour12:false}):''}
async function patchClient(store,c,patch){const before={...c};Object.assign(c,patch);const local=store.clients.find(x=>x.id===c.id);if(local)Object.assign(local,patch);const {error}=await supabase.from('clients').update(patch).eq('id',c.id);if(error){Object.assign(c,before);if(local)Object.assign(local,before);throw error}}
async function saveResult(store,c,outcome){const patch={remark:[cleanRemark(c.remark),marker(outcome)].filter(Boolean).join('\n')};if(outcome==='no_show')patch.status='absent';if(outcome.startsWith('opened_'))patch.status='active';await patchClient(store,c,patch)}
function resultCard(store,c,onDone=()=>{}){const root=document.createElement('div');root.className='tf-backdrop';root.innerHTML=`<div class="tf-card"><div class="tf-head"><div class="tf-title">📝 填寫試堂結果</div><button class="tf-close">×</button></div><div style="background:#f8fafc;border-radius:16px;padding:13px;margin-bottom:12px"><b>${esc(c.name)}</b> ${esc(c.phone||'')}<br>${esc(c.branch||'')} · ${fmt(c)}</div><div class="tf-actions"><button class="tf-btn" data-a="opened">✅ 開咗卡</button><div class="tf-sub"><button data-a="small">10點細卡</button><button data-a="big">35點大卡</button></div><button class="tf-btn" data-a="no_open">❌ 試堂後冇開卡</button><button class="tf-btn" data-a="no_show">🚫 冇出席</button><button class="tf-btn" data-a="move">📅 改咗其他日子</button><div class="tf-res"><input type="datetime-local"><button class="tf-save">確認新時間</button></div></div></div>`;document.body.appendChild(root);root.querySelector('.tf-close').onclick=()=>root.remove();root.querySelector('[data-a=opened]').onclick=()=>root.querySelector('.tf-sub').classList.toggle('show');root.querySelector('[data-a=move]').onclick=()=>root.querySelector('.tf-res').classList.toggle('show');const done=async o=>{try{await saveResult(store,c,o);root.remove();onDone()}catch(e){alert('更新失敗：'+e.message)}};root.querySelector('[data-a=small]').onclick=()=>done('opened_small');root.querySelector('[data-a=big]').onclick=()=>done('opened_big');root.querySelector('[data-a=no_open]').onclick=()=>done('no_open');root.querySelector('[data-a=no_show]').onclick=()=>done('no_show');root.querySelector('.tf-save').onclick=async()=>{const v=root.querySelector('input').value;if(!v)return alert('請選擇新日期及時間');try{await patchClient(store,c,{trial_date:v,status:'prospect',remark:cleanRemark(c.remark)});root.remove();onDone()}catch(e){alert(e.message)}}}
function taskCenter(store){const root=document.createElement('div');root.className='tf-backdrop';const waiting=store.clients.filter(ended);const future=store.clients.filter(c=>isToday(c)&&!hasResult(c)&&!ended(c));const rows=(arr,a)=>arr.map(c=>`<div class="tf-item"><div><div class="tf-item-name">${esc(c.name)} ${c.phone?'· '+esc(c.phone):''}</div><div class="tf-item-meta">${esc(c.branch||'')} · ${fmt(c)}</div></div><button data-id="${esc(c.id)}">${a}</button></div>`).join('');root.innerHTML=`<div class="tf-card"><div class="tf-head"><div class="tf-title">✅ 今日待辦</div><button class="tf-close">×</button></div><div class="tf-section">待填結果 ${waiting.length}</div><div class="tf-list">${rows(waiting,'填結果')||'<div class="tf-empty">暫時冇待處理結果 🎉</div>'}</div><div class="tf-section">今日稍後試堂 ${future.length}</div><div class="tf-list">${rows(future,'查看')}</div></div>`;document.body.appendChild(root);root.querySelector('.tf-close').onclick=()=>root.remove();root.querySelectorAll('.tf-item button').forEach(b=>b.onclick=()=>{const c=store.clients.find(x=>String(x.id)===b.dataset.id);if(c&&ended(c)){root.remove();resultCard(store,c,()=>setTimeout(()=>taskCenter(store),50))}})}
function parseTSV(text){const rows=[];let row=[],cell='',quoted=false;const s=String(text||'').replace(/\r\n?/g,'\n');for(let i=0;i<s.length;i++){const ch=s[i];if(ch==='"'){if(quoted&&s[i+1]==='"'){cell+='"';i++}else quoted=!quoted}else if(ch==='\t'&&!quoted){row.push(cell);cell=''}else if(ch==='\n'&&!quoted){row.push(cell);if(row.some(x=>String(x).trim()))rows.push(row);row=[];cell=''}else cell+=ch}row.push(cell);if(row.some(x=>String(x).trim()))rows.push(row);return rows}
function excelRows(text){return parseTSV(text).map(cols=>{while(cols.length<7)cols.push('');const day=String(cols[0]||'').trim(),branch=String(cols[1]||'').trim(),source=String(cols[2]||'').trim(),owner=String(cols[3]||'').trim();let name=String(cols[5]||'').replace(/\s*\n\s*/g,' & ').replace(/\s+/g,' ').trim();const item=String(cols[6]||'').replace(/\s+/g,' ').trim();const pm=(name+' '+item).match(/(?:\+?852[\s-]?)?[2-9]\d{3}[\s-]?\d{4}/);const phone=pm?pm[0].replace(/\D/g,'').replace(/^852(?=\d{8}$)/,''):'';name=name.replace(pm?.[0]||'','').replace(/[+·,，]\s*$/,'').trim();const keep=/(廣告)?試堂\s*(未開卡|冇開卡)|試堂後\s*(未開卡|冇開卡)/.test(item);return{day,branch,source,owner,name,phone,item,keep}}).filter(r=>r.name||r.item)}
function bestClient(store,r){const p=norm(r.phone),n=norm(r.name);if(p){const x=store.clients.find(c=>norm(c.phone)===p);if(x)return x}if(n){const exact=store.clients.find(c=>norm(c.name)===n);if(exact)return exact;const near=store.clients.filter(c=>{const cn=norm(c.name);return cn&&n.length>=4&&(cn.includes(n)||n.includes(cn))});if(near.length===1)return near[0]}return null}
function monthDate(day){const now=new Date();const d=Math.max(1,Math.min(31,parseInt(day,10)||now.getDate()));return `${now.getFullYear()}-${String(now.getMonth()+1).padStart(2,'0')}-${String(d).padStart(2,'0')}T12:00`}
async function saveBackfillRow(store,r){const found=bestClient(store,r),dt=monthDate(r.day),stamp=new Date(dt).toISOString();if(found){await patchClient(store,found,{trial_date:dt,branch:r.branch||found.branch||'觀塘',remark:[cleanRemark(found.remark),marker('no_open',stamp,'excel-backfill')].filter(Boolean).join('\n'),status:'prospect'});return}const {data:{session}}=await supabase.auth.getSession();const row={name:r.name,phone:r.phone||null,branch:r.branch||'觀塘',source:r.source||'補回試堂',status:'prospect',join_date:dt.slice(0,10),trial_date:dt,remark:marker('no_open',stamp,'excel-backfill'),owner_email:session?.user?.email};const {data,error}=await supabase.from('clients').insert(row).select().single();if(error)throw error;if(data)store.clients.unshift(data)}
function renderBatch(root,store,rows){const kept=rows.filter(r=>r.keep),list=root.querySelector('.tf-batch-list'),sum=root.querySelector('.tf-batch-summary'),save=root.querySelector('.tf-batch-save');if(!kept.length){list.innerHTML='<div class="tf-empty">未找到「試堂未開卡」記錄</div>';sum.textContent=`讀到 ${rows.length} 行，但 0 行需要補回`;save.style.display='none';return}list.innerHTML=kept.map((r,i)=>{const found=bestClient(store,r);return `<div class="tf-batch-row ${found?'':'bad'}" data-i="${i}"><div class="tf-batch-top"><div><div class="tf-batch-name">${esc(r.name)}</div><div class="tf-batch-meta">${esc(r.day)}日 · ${esc(r.branch||'未填分店')} · ${esc(r.source||'')} ${r.phone?'· '+esc(r.phone):''}<br>${esc(r.item)}</div></div><span class="tf-batch-tag">${found?'已配對':'新記錄'}</span></div><select data-branch><option ${r.branch==='觀塘'?'selected':''}>觀塘</option><option ${r.branch==='中環'?'selected':''}>中環</option><option ${r.branch==='佐敦'?'selected':''}>佐敦</option></select></div>`}).join('');sum.textContent=`共讀到 ${rows.length} 行 · 準確篩出 ${kept.length} 位試堂未開卡`;save.style.display='block';save.onclick=async()=>{if(!confirm(`確認一次過補回 ${kept.length} 筆？`))return;save.disabled=true;let ok=0,fail=0;for(let i=0;i<kept.length;i++){const el=list.querySelector(`[data-i="${i}"]`);kept[i].branch=el?.querySelector('[data-branch]')?.value||kept[i].branch;try{await saveBackfillRow(store,kept[i]);ok++}catch(e){fail++;el?.classList.add('bad')}}save.disabled=false;save.textContent=`完成：成功 ${ok} · 失敗 ${fail}`;if(!fail)setTimeout(()=>root.remove(),800)}}
function openBackfill(store){const root=document.createElement('div');root.className='tf-backdrop';root.innerHTML=`<div class="tf-card"><div class="tf-head"><div><div class="tf-title">⚡ 補回試堂記錄</div><div style="color:#64748b">電話唔係必填</div></div><button class="tf-close">×</button></div><div class="tf-filter-note">直接由 Google Sheets / Excel 複製整個範圍。支援儲存格內換行，系統只抽「試堂未開卡／試堂冇開卡」，其他記錄全部忽略。</div><div class="tf-smart" style="margin-top:10px"><label>📋 貼上試算表內容</label><textarea data-excel style="min-height:220px" placeholder="直接貼上 Excel / Google Sheets 多行資料"></textarea><button class="tf-save" data-analyse style="margin-top:8px">分析並篩選</button></div><div class="tf-batch-summary"></div><div class="tf-batch-list"></div><button class="tf-save tf-batch-save" style="display:none">確認並一次過儲存</button></div>`;document.body.appendChild(root);root.querySelector('.tf-close').onclick=()=>root.remove();root.querySelector('[data-analyse]').onclick=()=>renderBatch(root,store,excelRows(root.querySelector('[data-excel]').value))}
export function installTrialFollowUp(store){if(document.getElementById('tf-style'))return;const s=document.createElement('style');s.id='tf-style';s.textContent=css;document.head.appendChild(s);const tools=document.createElement('div');tools.className='tf-tools';tools.innerHTML='<button class="tf-tool tf-task">✅ 今日待辦 <span class="tf-badge" style="display:none"></span></button><button class="tf-tool tf-backfill">⚡ 補試堂</button>';document.body.appendChild(tools);tools.querySelector('.tf-task').onclick=()=>taskCenter(store);tools.querySelector('.tf-backfill').onclick=()=>openBackfill(store);const badge=tools.querySelector('.tf-badge');const check=()=>{tools.style.display=store.view==='dashboard'?'flex':'none';const p=store.clients.filter(ended);badge.textContent=p.length;badge.style.display=p.length?'inline-flex':'none'};setTimeout(check,500);setInterval(check,5000);window.addEventListener('focus',()=>setTimeout(check,80))}
